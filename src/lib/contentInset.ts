/** Shared left gutter for full-width content and horizontal rails. */
export const MOBILE_CONTENT_INSET_LEFT = "pl-4 lg:pl-24";

/** Page shell horizontal inset — left gutter on mobile, right gutter always. */
export const CONTENT_INSET_X = "px-4";

/** Show / movie detail pages: left gutter on all breakpoints (hero banner stays full width). */
export const SHOW_CONTENT_INSET_X = "pl-4 pr-3 sm:pr-4";

/** Alias for movie detail / watch content gutters (same as shows). */
export const MOVIE_CONTENT_INSET_X = SHOW_CONTENT_INSET_X;
