/** Mobile left gutter (hamburger layout); desktop flush to the sidebar. */
export const MOBILE_CONTENT_INSET_LEFT = "pl-4 lg:pl-0";

/** Page shell horizontal inset — left gutter on mobile, right gutter always. */
export const CONTENT_INSET_X = "pl-4 pr-3 sm:pr-4 lg:pl-0";

/** Show pages: left gutter on all breakpoints (hero banner stays full width). */
export const SHOW_CONTENT_INSET_X = "pl-2 pr-3 sm:pr-4";
