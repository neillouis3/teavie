/**
 * TV catalog cleanup: re-exports from src/lib/tvJpAnimePrune.js (canonical).
 * Scripts that cannot use ESM import should dynamic-import the src module instead.
 */
module.exports = require("../../src/lib/tvJpAnimePrune.js");
